[pytest]
asyncio_mode = auto
# Bez tego "custom_components.analog_meter_reader.*" (używane w
# test_coordinator.py/test_config_flow.py) jest nieimportowalne, gdy CI woła
# gołe `pytest tests/` zamiast `python -m pytest` (ten drugi sam dorzuca cwd
# na sys.path - stąd działało lokalnie, a nie w CI, dopóki tego nie było).
pythonpath = .
